# 示例配置文件，只能放占位符，禁止提交真实令牌。

# 默认 Apifox 访问令牌。
AI_PIPELINE_CREDENTIAL_APIFOX_TOKEN=replace_with_apifox_access_token

# 如果后续同一平台需要多套令牌，可以用 profile 隔离。
# 读取时 profile=team_a 会寻找这个变量。
AI_PIPELINE_CREDENTIAL_APIFOX_TEAM_A_TOKEN=replace_with_team_a_apifox_access_token
