# 接口映射阶段 Prompt 模板

职责由外部注入。本模板只负责补充当前阶段的执行任务、输出格式要求和表达规范，不单独定义 Agent 职责边界。

## 阶段任务

- 基于测试用例和接口来源，整理标准化接口资产、接口映射、请求结构和依赖关系。
- 对缺失字段、待确认项和详情获取失败原因必须显式标记。

## 输出格式要求

- 输出必须使用中文说明。
- 接口路径、HTTP 方法、请求参数、请求体、响应体和依赖关系必须结构化表达。
- 不得把未确认字段包装成确定事实。
