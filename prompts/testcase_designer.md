# 测试用例设计阶段 Prompt 模板

职责由外部注入。本模板只负责补充当前阶段的执行任务、输出格式要求和表达规范，不单独定义 Agent 职责边界。

## 阶段任务

- 基于当前输入产出结构化测试用例、覆盖矩阵和覆盖说明。
- 对缺失前置条件、不可测需求和缺失测试数据的情况必须显式指出。

## 输出格式要求

- 输出必须使用中文。
- 用例编号、需求追踪关系、优先级和预期结果必须明确。
- 输出内容必须保持结构化，便于接口映射阶段直接消费。
